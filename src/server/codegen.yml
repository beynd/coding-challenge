schema:
  - '../../schema.graphql'
generates:
  'types.ts':
    plugins:
      - typescript:
          skipTypename: true
      - typescript-resolvers

hooks:
  afterAllFileWrite:
    - prettier --write
